<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Name="BrowseItemsPage"
             x:Class="Game.Views.PickCharactersPage"
             Title="{Binding Title}">

    <ContentPage.Content>
        <StackLayout Style="{StaticResource OuterBoxStack}">

            <Frame Style="{StaticResource TitleFrame}">
                <Label Text="Assemble the team..." Style="{StaticResource CenterMessageMedStyle}"/>
            </Frame>

            <ScrollView Style="{StaticResource ScrollStyle}">

                <StackLayout Style="{StaticResource PaddedBox}">
                    
                </StackLayout>
                
            </ScrollView>

            <ImageButton x:Name="BeginBattleButton"
                Clicked="BattleButton_Clicked"
                IsEnabled="False"
                Source="begin_button.png"/>

        </StackLayout>
    </ContentPage.Content>
</ContentPage>